let payRate = 25.00;
let hoursWorked = 40;
if (hoursWorked <=40) {
    console.log(`no overtime`);
    console.log(payRate * hoursWorked);
} else {
    (payRate * 1.5) + hoursWorked  
}
let grossPay = payRate * hoursWorked;
